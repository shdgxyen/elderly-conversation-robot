version: "1.0.0"

dependencies:
  idf: ">=5.5,<6.0"
  # Keep the same codec-board release as Waveshare's ESP-IDF 5.5.3
  # Audio_Test example. It contains the exact ES8311/ES7210 GPIO map for
  # S3_AMOLED_1_43C.
  tempotian/codec_board: "==2.0.0"
  espressif/esp_codec_dev: "==1.5.4"
